---
sidebar_position: 6
toc_max_heading_level: 4
---

# Gesture Detector 

```mdx-code-block
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
```

Widget에서 Gesutre가 감지될 때 실행될 함수를 정의해줄 수 있음.
```dart
GestureDetector(
	onTap() {
		실행할 함수
	}
)
```
## Focus 해제하기 
다음 함수는 Focus를 해제해줌. 즉, 키보드를 입력하다가 onTap() {} 안에 실행할 함수로 넣어주면 키보드의 포커싱이 해제됨.
```dart
FocusScope.of(context).requestFocus(FocusNode());
```

## InkWell
Gesture가 선택이 될 때 spread 효과줌.<br></br>

<Tabs>
<TabItem value="Inkwell">
<div style={{textAlign: "center"}}>
<img src={require("./img/gesture-detector/inkwell.gif").default} style={{width: 300}}/>
</div>
</TabItem>
<TabItem value="Gesture Detector">

:::caution
아무리 tap을 해도 tap에 대한 Feedback이 없음.
:::

<div style={{textAlign: "center"}}>
<img src={require("./img/gesture-detector/gesture_detector.gif").default} style={{width: 300}}/>
</div>
</TabItem>
</Tabs>
